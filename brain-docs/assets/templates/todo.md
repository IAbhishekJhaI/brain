# TODO / Notes

> **Maintenance:** add caveats/risks/ideas as they surface, check off `[x]`
> and remove completed items, keep the severity tags — every session, as you
> work.

Things worth noting: action items, caveats, correctness/security
observations, and feature ideas that surfaced while working.

Tags: 🔴 action-required · 🟠 correctness/security · 🟡 known limitation
(by design) · 🟢 feature · 🔵 tech-debt. `(session)` = surfaced while
working · `(roadmap)` = imported from a plan/handoff doc.

---

## 🔴 Action required

<!-- Blocking items: deploy steps, migrations to run, config to set.
- [ ] **Short imperative title.** `(session)` — what, why it blocks, exact
      command or step to resolve. `path/to/file.py:123`.
-->

## 🟠 Correctness / security observations

<!-- Risks noticed while reading or working — verify & fix.
- [ ] **What's wrong.** `(session)` — the failure scenario, where it lives,
      and the sketch of a fix. `path/to/file.ts:45`.
-->

## 🟡 Known limitations (by design — remember these)

<!-- Deliberate simplifications a future session shouldn't "fix" blindly.
- [ ] **The limitation.** `(session)` — why it's acceptable today, and what
      would trigger revisiting it. `path/to/file.py`.
-->

## 🟢 Feature improvements

<!-- Ideas worth doing, roughly ranked.
- [ ] **The idea.** `(session)` — the value, and a sketch of the seams it
      would touch.
-->

## 🔵 Tech debt / cleanups

<!-- Non-urgent cleanups.
- [ ] **The cleanup.** `(session)` — what and where. `path/to/file.ts`.
-->
